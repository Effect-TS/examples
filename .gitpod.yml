tasks:
  - init: |
      yarn install
      curl -fsSL https://deno.land/x/install/install.sh | sh
      sudo ln -s /home/gitpod/.deno/bin/deno /bin/deno
      deno cache deno/src/main.ts

vscode:
  extensions: 
   - denoland.vscode-deno
